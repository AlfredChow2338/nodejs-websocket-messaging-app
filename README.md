# Node.js Web Socket Messaging App

## Usage

Run client side server on port 5000 with Python 3 HTTP server

```
python3 -m http.server --cgi 5000
```

Install dependencies on NPM

```
npm install
```

Run serve side server (by default port 3000)

```
npm run devStart
```